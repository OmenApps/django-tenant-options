"""Optional django-bolt adapter for django-tenant-options.

``django-bolt`` is experimental and optional. Install via
``pip install django-tenant-options[bolt]``. The adapter module is soft-guarded
so it imports cleanly even when django-bolt is not installed.
"""
